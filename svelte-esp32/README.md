# Quick Start Dev For Svelte

1. Install nodejs.
2. Open this folder in a terminal windows and run "npm install".
3. Launch the local server with "npm run dev".
4. To compile to a single file, run "npm run build".